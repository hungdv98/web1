<%= form_with(model: emoji, local: true) do |form| %>
  <% if emoji.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emoji.errors.count, "error") %> prohibited this emoji from being saved:</h2>
      <ul>
      <% emoji.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <h5><strong>format</strong>   :your_emo_code:</h5>
    <%= form.label :code %><br>
    <%= form.text_field :code %>
  </div>
  <div class="field">
    <%= form.label :emo %><br>
    <%= form.file_field :emo%>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

